version: 2

updates:
  # The plugin ships zero third-party dependencies (plain PHP, no Composer/npm),
  # so the only thing to keep current is the GitHub Actions used by CI/release.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ['*']
